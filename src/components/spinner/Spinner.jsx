import './spinner.scss';
import React from 'react';

const Spinner = () => {
  return (
    <div className="spinner">
      <div className="spinner__loader" />
    </div>
  );
};

export default Spinner;
